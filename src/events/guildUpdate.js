const GuildCfg = require('../database/schemas/GuildConfig');

module.exports = (bot) => {
	// bot.on('guildUpdate', async (server) => {
	// 	const serverCfg = await GuildCfg.findByIdAndUpdate(
	// 		{ guildId: id },
	// 		{
	// 			guildName: server.name,
	// 		}
	// 	);
	// });
};
